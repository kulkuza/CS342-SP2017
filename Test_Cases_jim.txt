Test Cases
1) Changing size of the array should create anew arrayof the new size with new elements.

2) Swapping elements in the arra should work. And access specific elements in the array should work.

3) When using a sorting algorithm, the proper array should be made for it.
